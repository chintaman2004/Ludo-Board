import 'package:flutter/material.dart';

class IPDScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("IPD Department")),
      body: Center(child: Text("IPD Module Placeholder")),
    );
  }
}