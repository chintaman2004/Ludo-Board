import 'package:flutter/material.dart';

class OPDScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("OPD Department")),
      body: Center(child: Text("OPD Module Placeholder")),
    );
  }
}